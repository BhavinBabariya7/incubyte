#include<iostream>
#include<bits/stdc++.h>
using namespace std;

void calculateCoordinate(char arr[],int &coOrdinates[]) {
    for(char a : arr ) {
        char ch;
        switch(ch) {
            case 'f':
                break;
            case 'b':
                break;
            case 'l':
                break;
            case 'r':
                break;
            case 'u':
                break;
            case 'd':
                break;
        }

    }
}

int main() {
    int coOrdinates[] = {0,0,0}; 
    char arr[] = {'f','r','u','b','l'};
    calculateCoordinate(arr,coOrdinates);

    //print answer

    return 0;
}